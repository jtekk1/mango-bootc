# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
      packages:
        - plymouth
        - plymouth-theme-solar

  - type: script
    scripts:
      - plymouth-setup.sh
